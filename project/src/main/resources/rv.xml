<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rvns">
	<select id="list" parameterType="integer" resultType="rv">
		select * from review where m_num=#{m_num} order by re_num
	</select>
	<insert id="insert" parameterType="rv">
		<selectKey keyProperty="re_num" order="BEFORE" resultType="integer">
			select nvl(max(re_num), 0) + 1 from review
		</selectKey>
		insert into review values (#{re_num}, #{re_con},
			#{re_grade}, sysdate, sysdate, #{member_id}, #{m_num}, 'n')
	</insert>
	<update id="delete" parameterType="integer">
		update review set re_del='y' where re_num=#{re_num}
	</update>
	<update id="update" parameterType="rv">
		update review set re_con=#{re_con}, re_grade=#{re_grade}, 
			re_update=sysdate where re_num=#{re_num}
	</update>
</mapper>