<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace xml을 구별하기 위한 이름 -->
<!-- resultMap는 table에 컬럼명과 class에 dept변수명이 다를 때 mapping하는 용도 -->
<mapper namespace="memberns">
	<select id="select" parameterType="string" resultType="member">
		select * from member where member_id = #{member_id}
	</select>
	<insert id="insert" parameterType="member">
		insert into member values(#{member_id}, #{member_password}, #{member_name}, #{member_gender},#{member_birth},
		#{member_email},#{member_number},#{member_addr},sysdate, 'n')
	</insert>
	<update id="update" parameterType="member">
		update member set member_name=#{member_name},member_password=#{member_password},member_gender=#{member_gender},member_birth=#{member_birth},member_email=#{member_email},member_number=#{member_number},member_addr=#{member_addr} where member_id=#{member_id}
	</update>
	<update id="delete" parameterType="string">
		update member set member_del='y' where member_id=#{member_id}
	</update>
</mapper>