<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reBoardns">
	<select id="list" parameterType="integer" resultType="reBoard">
		select * from reBoard where b_num=#{b_num} order by r_num
	</select>
	<insert id="insert" parameterType="reBoard">
		<selectKey keyProperty="r_num" order="BEFORE" resultType="integer">
			select nvl(max(r_num),0) + 1 from reBoard
		</selectKey>
		insert into reBoard values (#{r_num},#{b_num},#{member_id},#{r_text},sysdate,'n')
	</insert>
	<update id="update" parameterType="reBoard">
		update reBoard set r_text=#{r_text},r_date=sysdate where r_num=#{r_num}
	</update>
	<update id="delete" parameterType="integer">
		update reBoard set r_del='y' where r_num=#{r_num}
	</update>
	<select id="select_r_count" parameterType="integer" resultType="integer">
		select count(*) from reBoard where b_num=#{b_num}
	</select>
</mapper>